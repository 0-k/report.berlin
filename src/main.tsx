import React from 'react';
import ReactDOM from 'react-dom/client';
import BerlinReport from './components/BerlinReport';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BerlinReport />
  </React.StrictMode>
);
